# FastAPI and Server
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6

# Database
motor==3.3.2
pymongo==4.6.1
pydantic[email]==2.5.3

# Authentication & Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
google-auth>=2.48.1,<3.0.0  # verifies Google Sign-In ID tokens — see auth_service.authenticate_google

# Validation
pydantic==2.5.3
pydantic-settings==2.1.0
email-validator==2.1.0

# HTTP Client
httpx>=0.28.1
aiohttp==3.9.1

# Task Queue
celery==5.3.6
redis==5.0.1

# AI/ML — Groq is called directly over HTTP (see ai_service.py) using the
# httpx dependency already pinned above; no separate SDK needed.

# Utilities
duckduckgo-search>=5.3.1
python-dotenv==1.0.0
loguru==0.7.2
python-dateutil==2.8.2
feedparser==6.0.10
# IANA tz database for zoneinfo.ZoneInfo — python:3.11-slim has no system
# tzdata package, so ZoneInfo("Asia/Kolkata") (Bytes' day-scoping) would
# raise ZoneInfoNotFoundError without this.
tzdata==2024.1

# Testing
pytest==7.4.4
pytest-asyncio==0.23.3
pytest-cov==4.1.0
httpx>=0.28.1

# Security / sanitization
bleach==6.1.0

# Rate limiting
slowapi==0.1.9
limits==3.10.0

# CORS
cors==1.0.1

# Cloud Storage
google-cloud-storage>=2.14.0,<3.0.0
Pillow>=10.2.0,<11.0.0

# Observability — error tracking (no-ops until SENTRY_DSN is set, see config.py)
sentry-sdk[fastapi]>=2.0.0,<3.0.0

# Web Push (VAPID) — no-ops until VAPID_PUBLIC_KEY/VAPID_PRIVATE_KEY are set
pywebpush>=2.0.0,<3.0.0
